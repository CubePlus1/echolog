# EchoLog

Local-first activity recorder with a REST API, server-hosted Web UI, and `el` CLI.

Shell-capable agents should use the CLI as their tool surface. Run `el --help` for command help and use `--json` on supported commands for machine-readable output.
